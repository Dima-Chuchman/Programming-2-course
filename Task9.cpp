/*

Task 1.9

Ввести два дійсних числа записаних через пробіл в одному рядку та обчислити значення їх середнього арифметичного 
та середнього гармонічного. Результат вивести в науковому та десятковому представленні.

*/

//#include <stdio.h>
//#include <Windows.h>
//
//int main() {
//
//	SetConsoleCP(1251);
//	SetConsoleOutputCP(1251);
//
//	double x, y;
//
//	printf("Введіть значення x та y в один рядок: ");
//	scanf_s("%lf %lf", &x, &y);
//
//	double arithmetic_mean = (x + y) / 2.0;
//	double harmonic_mean = 2.0 / (1.0 / x + 1.0 / y);
//
//	printf("Середнє арифметичне (науковий формат): %e\n", arithmetic_mean);
//	printf("Середнє гармонічне (науковий формат): %e\n", harmonic_mean);
//
//	printf("Середнє арифметичне (десятковий формат): %.6f\n", arithmetic_mean);
//	printf("Середнє гармонічне (десятковий формат): %.6f\n", harmonic_mean);
//
//}